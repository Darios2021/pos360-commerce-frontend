node_modules
dist
.vscode
.DS_Store
.git
.gitignore
npm-debug.log
yarn.lock
pnpm-lock.yaml
# .env y .env.* SÍ se copian al build — Vite los lee para inyectar
# VITE_GOOGLE_MAPS_API_KEY y demás vars del bundle de producción.
# Si no se copia, el bundle queda con keys vacías y Maps falla en prod.
.env.local

# Android / Capacitor: no se necesitan para el build del frontend web
android
capacitor.config.json
*.aab
*.keystore
# El APK distribuible vive en public/ y SÍ tiene que ir al bundle
android/app/build/**/*.apk
